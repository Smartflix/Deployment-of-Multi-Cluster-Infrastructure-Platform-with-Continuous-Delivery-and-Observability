apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "db.fullname" . }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "db.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "db.name" . }}
    spec:
      containers:
        - name: postgres
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 5432
